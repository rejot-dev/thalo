{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["grammar.js", "src/scanner.c", "bindings/**"],
      "outputs": ["src/**", "build/**"]
    }
  }
}
