---
"@rejot-dev/thalo": patch
"@rejot-dev/thalo-cli": patch
---

Fix WASM fallback not activating when native tree-sitter bindings are unavailable; add git hash and
parser backend to `--version` output.
